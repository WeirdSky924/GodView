### 1. 核心理念：从“写小说”到“推演世界”

你的核心目标是利用大语言模型（LLM）将小说创作转变为生态模拟。系统自动生成带有特定性格和目标的“角色 Agent”，通过它们在特定世界观下的互动，自下而上地涌现出剧情，并最终由系统统筹生成小说文本。前期由我们进行初步设定，随后配合 Agent 进行修改完善。

### 2. 架构演进与核心机制

为了保证小说的好看、不跑题以及不穿帮，我们在基础架构上增加了以下关键设计：

- **初始设定：**世界初始设定以及角色初始设定由设定 Agent 与用户共同配合完善。
- **记忆与世界锚定：** 抛弃纯文本上下文，使用图向量数据库和硬编码规则引擎（JSON状态）来管理角色的长期记忆、好感度、物品归属和空间坐标，防止 AI 产生幻觉。
- **主要角色单独发展：**核心角色信息单独存储 Agent ，然后可以自行发展，随后将发展内容发送给导演，由导演决定发展是否合理，并且让导演将发展内容结合进大世界发展中。
- **世界生成机：**
  - **引入“造物主助理” (ProcGen Agent)**：当定位系统发现角色进入了空白区域（数据库中无记录），立即唤醒该 Agent。它会根据当前的大世界观规则，随机生成（Procedural Generation）这个新区域的 JSON 数据（地形、遭遇怪物或奇遇、新 NPC 设定），自动存入数据库，然后角色 Agent 才能继续交互。
- **导演与编剧分离：** 不让角色 Agent 自由发挥到失控。设立了层级分明的“导演系统”：
  - **剧情总结员（Summarizer）：**它在后台将短期的、繁杂的对话记录压缩成干练的“事件摘要”。**当检测到角色对话中存在微妙的“潜台词”时，会进行特殊标记，允许后续的 Writer Agent 直接访问原始对话片段，防止信息失真。**
  - **总编剧 (Master Plotter)：** 把控主线进度，决定何时推进大剧情。**同时负责提前埋设“即将发生的事件”（如：“三刻钟后会有刺客”），为后续可能发生的强制剧情推进做铺垫。**
  - **伏笔管理员 (Hook Manager)：** 负责在日常互动中埋设伏笔，并在关键时刻强制提取伏笔交给文本生成器回收。
  - **内容执行官 (Writer Agent)：** 将意图、动作和伏笔，润色成有小说质感的连贯文本。强制要求 AI 使用“展示，而不是告知”（Show, Don't Tell）的技巧，规定描写比例。**在此阶段加入“风格一致性检查”环节，确保新生成的文本与前一章的文风绝对连贯。**
- **状态机控制与章节判定：**
  - 把小说剧情看作带有条件约束的“节点”。主线任务和未回收的伏笔没达标前，系统不会进入下一个大章节。
  - **章节结束判定器：** 专属 Agent 负责评估“本章信息增量是否足够”和“悬念是否已埋设”，达标后才允许收尾。
- **建立“口癖词典”与“性格审查” (Voice Embedding & Guardrails)**
  - **词汇与样本库限制：** 在数据库中为每个角色配置特定的“词汇表”（Lexicon）和“禁用语”，**并建立“典型台词样本库”（5-10句）。同时引入“角色声音嵌入”（Voice Embedding）技术，为每个角色生成独立的向量表示。**
  - **客观审查节点：** 在 LangGraph 中加入**角色 OOC 审查节点**。生成台词前先对齐声音向量，并通过与样本库的**向量相似度比对**来进行客观判定，避免依赖大模型的主观判断导致审查失效。
- **无效交互与“死锁”处理 (Agent Deadlocks & Interference Pool)：**
  - 设置“交互超时”与“强制推进”。在 LangGraph 中设定状态转移的阈值（如交互超过 5 轮无信息增量）。
  - 一旦触发阈值，导演 Agent 行使“Cut”权力。为了避免机械降神，**系统将从预设的“合理干扰事件池”（依据当前场景类型如密室、野外动态生成）中抽取打断事件，结合总编剧提前埋设的线索，顺理成章地推进剧情。**
- **“创世神”干预机制 (Human-in-the-Loop)：** 赋予你随时接管系统的能力。你可以暂停推演（断点）、实时监控 Agent、修改人物属性、回档重写，甚至“降临”角色。
  - **干预日志与数据分析：** 后台记录你每次修改的内容与时间，分析“哪些人为干预让剧情数据更好看”。
  - **干预影响范围提示：** 当你手动修改某项设定时，系统会自动计算“爆炸半径”并提示警报（如：“此修改将影响以下 3 个伏笔和 2 条人物关系”）。
- **配套辅助体验模块：**
  - **读者体验模拟器：** 设立专门的 Agent 扮演“首次阅读的挑剔读者”，对生成章节的节奏和悬念吸引力打分。
  - **版本差异工具（Diff Tool）：** 为回档操作提供可视化的剧情树对比，让你清晰看出不同版本走向的细微差别。

### 3. 终极技术栈蓝图

为了支撑上述复杂庞大的系统，我们梳理出了一套基于 Python 和现代 Web 技术的高级开发栈（已针对架构痛点进行优化）：

| **模块类别**       | **核心技术选型**                               | **负责的具体功能**                                           |
| ------------------ | ---------------------------------------------- | ------------------------------------------------------------ |
| **控制与编排层**   | **LangChain (原型) -> LangGraph (完全体)**     | 整个系统的心脏。前期使用 LangChain 快速验证核心工作流，后期迁移至 LangGraph 处理复杂的多 Agent 流转控制、状态机管理和“断点拦截”。 |
| **大模型引擎**     | **多模型路由架构 (Model Router)**              | 底层文本与逻辑生成。不再绑定单一模型，支持动态路由（如逻辑推演调用最新版 Claude 3.5/Opus，日常对话生成调用 GPT-4o 或本地开源模型以控制成本）。 |
| **关系/状态存储**  | **PostgreSQL (Supabase)**                      | 存储角色的基础面板（姓名、属性）、世界法则规则，以及每一章生成后的全量“世界快照 JSON”（用于回档）。 |
| **逻辑与记忆存储** | **NebulaGraph (或类似支持向量的统一图数据库)** | **替代双库方案**。将知识图谱（人物关系/伏笔追踪）与向量搜索（语义记忆）统一在一个数据库内，大幅降低数据同步复杂度和跨库查询延迟。 |
| **后端与通信**     | **FastAPI** + **WebSocket**                    | 处理大模型异步调用，并通过 WebSocket 将 Agent 的实时思考过程推送到前端监控面板。 |
| **前端管理控制台** | **React** + **React Flow **+ **Vite**          | 提供可视化界面。包含剧情分支树视图、数据库状态修改器、版本 Diff 对比界面以及实时干预控制台。 |
| **可观测性**       | **LangSmith**                                  | 调试工具，用于追踪每个 Agent 每一步的 Prompt，查看干预日志，排查剧情崩坏根源及 Token 消耗。 |

通过这套架构，你不仅是在开发一个写作软件，而是在打造一个**受控的数字沙盒**。你可以设定初始参数，让它自动演化，同时又稳坐在“造物主”的监控台前，确保每一条伏笔都能完美闭环，每一段主线都扣人心弦。