# GodView 前端操作文档

本文档详细介绍 GodView AI 小说生成系统的前端界面操作方法和功能说明。

---

## 目录

- [1. 系统概述](#1-系统概述)
- [2. 技术架构](#2-技术架构)
- [3. 界面导航](#3-界面导航)
- [4. 功能模块详解](#4-功能模块详解)
  - [4.1 仪表盘](#41-仪表盘)
  - [4.2 角色管理](#42-角色管理)
  - [4.3 角色声音](#43-角色声音)
  - [4.4 世界管理](#44-世界管理)
  - [4.5 剧情管理](#45-剧情管理)
  - [4.6 伏笔管理](#46-伏笔管理)
  - [4.7 干预日志](#47-干预日志)
  - [4.8 读者模拟](#48-读者模拟)
  - [4.9 可视化工作台](#49-可视化工作台)
  - [4.10 章节判定器](#410-章节判定器)
  - [4.11 导演模式](#411-导演模式)
  - [4.12 小说编辑器](#412-小说编辑器)
  - [4.13 版本对比](#413-版本对比)
  - [4.14 系统设置](#414-系统设置)
  - [4.15 项目初始化](#415-项目初始化)
- [5. API 接口说明](#5-api-接口说明)
- [6. 常见操作流程](#6-常见操作流程)
- [7. 故障排除](#7-故障排除)

---

## 1. 系统概述

GodView 是一个 AI 驱动的小说生成系统，前端基于 React + TypeScript 构建，提供完整的小说创作管理界面。

### 核心功能

- **角色管理**：创建和管理小说角色，支持声音样本训练
- **世界构建**：定义世界观、区域和设定
- **剧情管理**：章节创建、编辑和组织
- **伏笔管理**：伏笔埋设、追踪和回收
- **导演模式**：AI Agent 协作生成内容
- **读者模拟**：预测读者体验和反馈
- **版本控制**：快照管理和版本对比

### 系统要求

- Node.js 20+
- 现代浏览器（Chrome、Firefox、Edge、Safari）
- 后端服务运行中（端口 8000）

---

## 2. 技术架构

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3.1 | UI 框架 |
| TypeScript | 5.6.2 | 类型系统 |
| Vite | 5.4.10 | 构建工具 |
| Tailwind CSS | 3.4.14 | 样式框架 |
| React Router | 6.28.0 | 路由管理 |
| Zustand | 5.0.1 | 状态管理 |
| Axios | 1.7.7 | HTTP 请求 |
| ReactFlow | 11.11.4 | 流程图可视化 |
| Lucide React | 0.454.0 | 图标库 |

### 项目结构

```
frontend/
├── src/
│   ├── api/              # API 请求模块
│   │   ├── bootstrap.ts  # 引导流程接口
│   │   ├── chapters.ts   # 章节管理接口
│   │   ├── characters.ts # 角色管理接口
│   │   ├── client.ts     # Axios 客户端配置
│   │   ├── config.ts     # 系统配置接口
│   │   ├── director.ts   # 导演模式接口
│   │   ├── interventions.ts # 干预日志接口
│   │   ├── projects.ts   # 项目管理接口
│   │   ├── simulation.ts # 模拟接口
│   │   ├── time.ts       # 时间控制接口
│   │   ├── visualization.ts # 可视化接口
│   │   └── worlds.ts     # 世界管理接口
│   ├── components/       # 组件目录
│   │   ├── ui/           # 通用 UI 组件
│   │   ├── analytics/    # 分析组件
│   │   ├── bootstrap/    # 引导流程组件
│   │   ├── editor/       # 编辑器组件
│   │   ├── simulation/   # 模拟组件
│   │   ├── time/         # 时间控制组件
│   │   └── world/        # 世界视图组件
│   ├── hooks/            # 自定义 Hooks
│   ├── pages/            # 页面组件
│   ├── types/            # TypeScript 类型定义
│   ├── App.tsx           # 应用入口
│   ├── main.tsx          # 渲染入口
│   └── index.css         # 全局样式
├── public/               # 静态资源
├── index.html            # HTML 入口
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
├── tailwind.config.js    # Tailwind 配置
└── package.json          # 项目配置
```

---

## 3. 界面导航

### 侧边栏菜单

系统采用左侧固定导航栏布局，包含以下菜单项：

| 图标 | 菜单项 | 路径 | 说明 |
|------|--------|------|------|
| 🏠 | 仪表盘 | `/` | 系统概览和快速入口 |
| 👥 | 角色管理 | `/characters` | 创建和管理角色 |
| 🎤 | 角色声音 | `/character-voice` | 角色声音样本管理 |
| 🌍 | 世界管理 | `/worlds` | 世界观设定 |
| 📖 | 剧情管理 | `/plots` | 章节管理 |
| 🎯 | 伏笔管理 | `/hooks` | 伏笔追踪 |
| 🎛️ | 干预日志 | `/interventions` | 干预记录和评估 |
| 👁️ | 读者模拟 | `/simulator` | 读者体验预测 |
| 🔗 | 可视化工作台 | `/visualize` | 流程图可视化 |
| ✅ | 章节判定器 | `/chapter-evaluator` | 章节结束判定 |
| 🎬 | 导演模式 | `/director` | AI Agent 控制台 |
| 📚 | 小说编辑器 | `/novel` | 小说内容编辑 |
| 📊 | 版本对比 | `/diff` | 版本差异对比 |
| ⚙️ | 系统设置 | `/settings` | AI 配置 |

---

## 4. 功能模块详解

### 4.1 仪表盘

**路径**：`/`

**功能说明**：

仪表盘是系统的主页，提供项目概览和快速入口。

**界面元素**：

1. **统计卡片**
   - 角色数量：显示已创建的角色总数
   - 世界设定：显示已创建的世界数量
   - 已生成章节：显示章节数量
   - AI 助手：显示可用的 Agent 数量（固定为 6 个）

2. **快速入口**
   - **开始创作**：点击进入导演模式
   - **配置系统**：点击进入系统设置

3. **Agent 状态概览**
   - Summarizer（剧情总结员）
   - Master Plotter（总编剧）
   - Hook Manager（伏笔管理员）
   - Writer（内容执行官）
   - Evaluator（剧情评估员）
   - Character Agent（角色演绎）

**操作步骤**：

1. 打开浏览器访问 `http://localhost:5173`
2. 查看系统统计信息
3. 点击快速入口卡片进入对应功能

---

### 4.2 角色管理

**路径**：`/characters`

**功能说明**：

创建、编辑和管理小说角色，支持详细的角色属性设置和声音样本管理。

**界面布局**：

左侧为角色列表（网格布局），右侧为声音样本管理面板。

**角色属性**：

| 属性 | 类型 | 说明 |
|------|------|------|
| 名称 | 文本 | 角色名称（必填） |
| 角色定位 | 文本 | 如：主角、反派、配角 |
| 状态 | 选择 | 活跃/不活跃/已故 |
| 描述 | 多行文本 | 角色基本情况描述 |
| 性格特点 | 多行文本 | 性格、习惯、口头禅 |
| 说话风格 | 文本 | 语言风格描述 |
| 常用词汇 | 标签 | 用逗号分隔的词汇列表 |
| 禁用词 | 标签 | 角色不应使用的词汇 |
| 预置声音样本 | 多行文本 | 每行一条典型台词 |

**操作步骤**：

1. **创建角色**
   - 点击右上角「新增角色」按钮
   - 填写角色信息表单
   - 点击「创建角色」保存

2. **编辑角色**
   - 点击角色卡片上的「编辑」按钮
   - 修改表单内容
   - 点击「保存修改」

3. **删除角色**
   - 点击角色卡片上的「删除」按钮
   - 确认删除操作

4. **管理声音样本**
   - 点击角色卡片选中该角色
   - 在右侧面板添加/检索声音样本
   - 点击「同步向量」将样本向量化

**声音样本功能**：

- **新增台词样本**：输入代表性台词和上下文
- **语义检索**：输入待对比台词，查找相似样本
- **同步向量**：将声音样本向量化存储到 Qdrant

---

### 4.3 角色声音

**路径**：`/character-voice`

**功能说明**：

专门用于角色声音样本的批量管理和风格分析。

---

### 4.4 世界管理

**路径**：`/worlds`

**功能说明**：

创建和管理小说世界观设定。

**世界属性**：

| 属性 | 类型 | 说明 |
|------|------|------|
| 名称 | 文本 | 世界名称（必填） |
| 世界类型 | 选择 | 奇幻/科幻/历史/现代/其他 |
| 故事基调 | 选择 | 严肃/轻松/黑暗/幽默/史诗 |
| 描述 | 多行文本 | 世界观详细描述 |

**操作步骤**：

1. 点击「新建世界」按钮
2. 填写世界名称、类型、基调和描述
3. 点击「保存」创建世界
4. 在世界卡片上点击「编辑」或「删除」进行管理

---

### 4.5 剧情管理

**路径**：`/plots`

**功能说明**：

管理小说章节，支持创建、编辑、状态管理和字数统计。

**章节属性**：

| 属性 | 类型 | 说明 |
|------|------|------|
| 标题 | 文本 | 章节标题（必填） |
| 状态 | 选择 | 草稿/已发布/已归档 |
| 内容 | 多行文本 | 章节正文 |

**界面功能**：

- 表格展示章节列表
- 显示序号、标题、状态、字数、最后修改时间
- 支持编辑和删除操作

**操作步骤**：

1. 点击「新增章节」按钮
2. 输入章节标题
3. 选择状态（默认为草稿）
4. 可选填写章节内容
5. 点击「创建」保存

---

### 4.6 伏笔管理

**路径**：`/hooks`

**功能说明**：

管理小说中的伏笔线索，追踪埋设、触发、回收状态。

**伏笔属性**：

| 属性 | 类型 | 说明 |
|------|------|------|
| 标题 | 文本 | 伏笔名称（必填） |
| 类型 | 选择 | 伏笔/角色线索/剧情线索/物品线索/地点线索 |
| 优先级 | 选择 | 普通/重要/高优先级 |
| 描述 | 多行文本 | 伏笔内容说明 |
| 状态 | 选择 | 已埋设/已触发/已回收/已废弃 |

**状态筛选**：

- 全部
- 已埋设
- 已触发
- 已回收
- 已废弃

**操作步骤**：

1. 点击「新建伏笔」按钮
2. 填写标题、类型、优先级和描述
3. 点击「创建」保存
4. 悬停在伏笔卡片上，点击「变更状态」更新状态
5. 点击「编辑」修改伏笔内容

---

### 4.7 干预日志

**路径**：`/interventions`

**功能说明**：

记录和分析对剧情的人工干预行为，评估干预效果。

**干预类型**：

| 类型 | 说明 |
|------|------|
| 角色修改 | 修改角色设定或行为 |
| 剧情变更 | 改变剧情走向 |
| 伏笔调整 | 修改伏笔设置 |
| 关系变更 | 改变角色间关系 |
| 世界设定修改 | 修改世界观设定 |

**统计卡片**：

- 总干预次数
- 正面效果（评分 ≥ 4）
- 中性效果（评分 = 3）
- 负面效果（评分 < 3）

**操作步骤**：

1. 点击「记录干预」按钮
2. 选择干预类型
3. 选择关联快照
4. 填写干预描述
5. 点击「保存」
6. 在干预记录上点击「查看影响」
7. 填写效果评分（1-5星）和备注
8. 点击「保存评估」

---

### 4.8 读者模拟

**路径**：`/simulator`

**功能说明**：

模拟读者阅读体验，预测沉浸度、悬念指数和留存率。

**评估维度**：

| 维度 | 说明 |
|------|------|
| 沉浸度评分 | 读者投入程度（0-5分） |
| 悬念指数 | 紧张感程度（0-100%） |
| 预计留存率 | 读者继续阅读概率（0-100%） |
| 故事节奏 | 叙事节奏评价 |
| 角色塑造 | 角色刻画深度 |
| 剧情连贯性 | 情节逻辑一致性 |

**情感弧线**：

可视化展示情绪波动曲线，包括：
- 情绪波动
- 投入程度
- 紧张感

**操作步骤**：

1. 从下拉菜单选择章节
2. 点击「开始阅读模拟」按钮
3. 等待分析完成
4. 查看各项评分和反馈
5. 关注高亮时刻提示

---

### 4.9 可视化工作台

**路径**：`/visualize`

**功能说明**：

以流程图形式可视化展示工作流、剧情树和版本树。

**视图类型**：

1. **工作流**：展示 Agent 工作流程节点
2. **剧情树**：展示剧情分支结构
3. **版本树**：展示快照版本历史

**操作步骤**：

1. 从顶部下拉菜单选择世界
2. 点击标签切换视图类型
3. 使用鼠标拖拽移动画布
4. 使用滚轮缩放视图
5. 使用右下角控件控制视图

**ReactFlow 功能**：

- 缩放：滚轮或控件按钮
- 平移：鼠标拖拽
- 小地图：右下角缩略图导航

---

### 4.10 章节判定器

**路径**：`/chapter-evaluator`

**功能说明**：

评估章节是否适合收尾，从四个维度给出建议。

**评估维度**：

| 维度 | 说明 | 高分阈值 |
|------|------|----------|
| 信息增量 | 新信息的引入量 | ≥ 75% |
| 悬念埋设 | 伏笔和悬念设置 | ≥ 75% |
| 节奏控制 | 叙事节奏把控 | ≥ 75% |
| 完整度 | 章节完整性 | ≥ 75% |

**判定结果**：

- **建议可以收尾**：各项指标达标，可以结束章节
- **建议暂不收尾**：需要继续完善，显示后续建议

**操作步骤**：

1. 选择要评估的章节
2. 点击「开始评估」按钮
3. 查看四个维度的评分
4. 阅读判定结果和后续建议

---

### 4.11 导演模式

**路径**：`/director`

**功能说明**：

导演模式是系统的核心功能，通过 AI Agent 协作自动生成小说内容。

**界面布局**：

- 左侧：Agent 状态、控制面板、对话输入
- 右侧：实时日志
- 底部：运行时状态、工作流图、版本树

**会话控制**：

1. 输入会话 ID
2. 点击「开始生成」启动会话
3. 点击「停止生成」暂停
4. 点击「重置」清空状态

**Agent 列表**：

| Agent | 中文名 | 功能 |
|-------|--------|------|
| Summarizer | 剧情总结员 | 总结剧情进展 |
| Master Plotter | 总编剧 | 规划剧情走向 |
| Hook Manager | 伏笔管理员 | 管理伏笔埋设与回收 |
| Writer | 内容执行官 | 生成正文内容 |
| Evaluator | 剧情评估员 | 评估内容质量 |
| Character Agent | 角色演绎 | 模拟角色行为 |
| ProcGen | 世界生成 | 生成世界元素 |

**对话/工作流输入**：

| 字段 | 说明 | 格式 |
|------|------|------|
| speaker_id | 说话者ID | narrator 或角色ID |
| present_characters | 在场角色 | 逗号分隔的角色ID |
| context | 场景上下文 | 文本描述 |
| intents | 意图列表 | 逗号或换行分隔 |
| environment | 环境描写 | 文本描述 |
| character_moods | 角色情绪 | 每行一条：角色ID:情绪 |

**快速操作**：

- **推进剧情**：触发剧情推进
- **管理伏笔**：检查伏笔状态
- **检查章节结束**：判断是否应结束当前章节

**生成按钮**：

- **生成对话**：生成角色对话
- **生成叙事**：生成叙事段落
- **执行工作流**：执行完整工作流周期

**声音审查面板**：

显示最新生成的对话及其声音审查结果：
- 声音上下文：向量检索信息
- OOC 审查：角色一致性检查
- 自动重写结果：AI 修正建议

**叙事闭环面板**：

显示最新生成的叙事内容：
- 叙事内容
- 叙事声音审查
- 叙事自动重写

**第三阶段控制台**：

- 区域生成提示：输入探索方向
- 生成区域：创建新世界区域
- 创建分支快照：保存当前状态
- 回档到所选快照：恢复到指定版本

**运行时状态**：

- 当前阶段
- 当前快照 ID
- 主线进度
- 区域数量
- 已埋设/已回收伏笔数

---

### 4.12 小说编辑器

**路径**：`/novel`

**功能说明**：

专业的小说内容编辑界面，支持多章节管理和导出。

**界面布局**：

- 左侧：章节目录
- 右侧：编辑/预览区

**编辑功能**：

- 编辑/预览模式切换
- 自动保存提示
- 字数统计
- 章节导航（上一章/下一章）

**导出选项**：

| 格式 | 说明 |
|------|------|
| 导出全文 TXT | 所有章节合并导出为 TXT |
| 导出当前章节 | 当前章节导出为 TXT |
| 导出 Markdown | 所有章节导出为 Markdown |

**操作步骤**：

1. **创建章节**
   - 点击「新建章节」按钮
   - 输入章节标题
   - 点击「创建」

2. **编辑章节**
   - 点击左侧目录选择章节
   - 点击「编辑」按钮进入编辑模式
   - 输入内容
   - 点击「保存」

3. **导出内容**
   - 悬停在「导出」按钮上
   - 选择导出格式

---

### 4.13 版本对比

**路径**：`/diff`

**功能说明**：

对比不同版本之间的差异，支持快照对比和章节对比。

---

### 4.14 系统设置

**路径**：`/settings`

**功能说明**：

配置 AI 模型和 Embedding 服务。

**Embedding 配置**：

| 参数 | 说明 |
|------|------|
| Provider | 服务类型（OpenAI/Sentence Transformers/Ollama） |
| 模型名称 | 使用的模型 |
| 向量维度 | 向量维度数 |
| API Key | API 密钥（如需要） |
| 基础 URL | 服务地址 |

**LLM 配置**：

| 参数 | 说明 |
|------|------|
| Provider | 模型提供商 |
| 模型名称 | 使用的模型 |
| 基础 URL | API 地址 |
| Temperature | 生成温度（0-1） |
| Max Tokens | 最大输出长度 |
| API Key | API 密钥 |

**操作步骤**：

1. 选择 Provider 类型
2. 系统自动填充默认值
3. 根据需要修改配置
4. 点击「测试连接」验证配置
5. 点击「保存配置」保存

---

### 4.15 项目初始化

**路径**：`/bootstrap`

**功能说明**：

通过引导流程快速初始化新项目，包括设定对话和大纲上传。

**初始化流程**：

1. **项目选择**
   - 选择现有项目
   - 或创建新项目
   - 点击「开始引导流程」

2. **设定 Agent 对话**
   - 与 AI 对话描述小说设定
   - 包括世界观、角色、故事主线等
   - Agent 会引导完善设定

3. **大纲上传**
   - 支持三种方式：
     - 粘贴文本
     - 上传 TXT 文件
     - 上传 Markdown 文件

4. **种子确认**
   - 系统提取结构化种子数据
   - 检查并确认种子内容
   - 可返回修改

5. **运行初始化**
   - 自动创建世界、角色、Agent 等
   - 显示进度

6. **完成**
   - 进入 Director 模式开始创作
   - 或进入世界观测查看模拟状态

---

## 5. API 接口说明

### 基础配置

- **Base URL**：`/api`
- **认证方式**：Bearer Token（如配置）
- **Content-Type**：`application/json`

### 主要接口模块

#### 章节接口 (`/api/chapters.ts`)

```typescript
// 获取章节列表
GET /api/chapters

// 创建章节
POST /api/chapters

// 更新章节
PUT /api/chapters/:id

// 删除章节
DELETE /api/chapters/:id

// 章节评估
POST /api/chapters/:id/evaluate

// 读者模拟
POST /api/chapters/:id/simulate-reader
```

#### 角色接口 (`/api/characters.ts`)

```typescript
// 获取角色列表
GET /api/characters

// 创建角色
POST /api/characters

// 更新角色
PUT /api/characters/:id

// 删除角色
DELETE /api/characters/:id

// 获取声音样本
GET /api/characters/:id/voice-samples

// 添加声音样本
POST /api/characters/:id/voice-samples

// 同步向量
POST /api/characters/:id/sync-voice

// 检索声音样本
POST /api/characters/:id/search-voice
```

#### 世界接口 (`/api/worlds.ts`)

```typescript
// 获取世界列表
GET /api/worlds

// 创建世界
POST /api/worlds

// 更新世界
PUT /api/worlds/:id

// 删除世界
DELETE /api/worlds/:id
```

#### 导演接口 (`/api/director.ts`)

```typescript
// WebSocket 连接
WS /api/ws/connect/:sessionId

// 获取状态
GET /api/director/:sessionId/state

// 获取快照树
GET /api/director/:worldId/snapshots/tree

// 获取工作流图
GET /api/director/:sessionId/workflow-graph
```

#### 配置接口 (`/api/config.ts`)

```typescript
// 获取 Embedding Providers
GET /api/config/embedding/providers

// 获取 Embedding 配置
GET /api/config/embedding

// 更新 Embedding 配置
PUT /api/config/embedding

// 测试 Embedding 配置
POST /api/config/embedding/test

// 获取 LLM Providers
GET /api/config/llm/providers

// 获取 LLM 配置
GET /api/config/llm

// 更新 LLM 配置
PUT /api/config/llm

// 测试 LLM 配置
POST /api/config/llm/test
```

---

## 6. 常见操作流程

### 6.1 创建新项目

1. 访问 `/bootstrap` 页面
2. 创建新项目或选择现有项目
3. 与设定 Agent 对话描述需求
4. 上传大纲文件
5. 确认种子数据
6. 等待初始化完成
7. 进入 Director 模式开始创作

### 6.2 手动创建角色和世界

1. 访问 `/worlds` 创建世界观
2. 访问 `/characters` 创建角色
3. 为角色添加声音样本
4. 访问 `/plots` 创建章节框架
5. 访问 `/hooks` 规划伏笔

### 6.3 使用导演模式生成内容

1. 访问 `/director` 页面
2. 输入会话 ID 并连接
3. 配置对话参数
4. 点击「执行工作流」
5. 查看生成结果和声音审查
6. 根据需要调整参数继续生成

### 6.4 评估和优化章节

1. 访问 `/chapter-evaluator`
2. 选择章节进行评估
3. 查看四个维度评分
4. 根据建议优化内容
5. 使用读者模拟验证效果

### 6.5 导出作品

1. 访问 `/novel` 小说编辑器
2. 确认所有章节内容完整
3. 点击「导出」选择格式
4. 下载生成的文件

---

## 7. 故障排除

### 7.1 页面无法加载

**症状**：浏览器显示空白或错误页面

**解决方案**：

1. 检查前端服务是否运行：`npm run dev`
2. 检查浏览器控制台错误
3. 清除浏览器缓存
4. 确认访问地址正确：`http://localhost:5173`

### 7.2 API 请求失败

**症状**：操作时出现红色错误提示

**解决方案**：

1. 检查后端服务是否运行：访问 `http://localhost:8000/health`
2. 检查 `.env` 配置是否正确
3. 检查数据库连接：`docker compose ps`
4. 查看后端日志排查错误

### 7.3 WebSocket 连接失败

**症状**：导演模式显示「未连接」

**解决方案**：

1. 确认后端 WebSocket 服务正常
2. 检查会话 ID 是否有效
3. 刷新页面重试
4. 检查浏览器是否阻止 WebSocket

### 7.4 AI 生成无响应

**症状**：点击生成按钮后无反应或报错

**解决方案**：

1. 检查 LLM API Key 配置
2. 验证 API 服务可用性
3. 检查模型名称是否正确
4. 查看后端日志确认错误原因

### 7.5 声音样本同步失败

**症状**：点击「同步向量」后失败

**解决方案**：

1. 检查 Qdrant 服务：`docker compose ps`
2. 检查 Embedding 配置
3. 确认模型已下载（Sentence Transformers）
4. 查看 Qdrant 控制台：`http://localhost:6333/dashboard`

---

## 附录

### A. 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl + S | 保存当前编辑内容 |
| Esc | 关闭模态框 |

### B. 状态码说明

| 状态 | 颜色 | 说明 |
|------|------|------|
| idle | 灰色 | 待命中 |
| working | 蓝色（闪烁） | 工作中 |
| completed | 绿色 | 已完成 |
| error | 红色 | 出错 |

### C. 章节状态

| 状态 | 颜色 | 说明 |
|------|------|------|
| draft | 黄色 | 草稿 |
| published | 绿色 | 已发布 |
| archived | 灰色 | 已归档 |

### D. 伏笔状态

| 状态 | 颜色 | 说明 |
|------|------|------|
| planted | 蓝色 | 已埋设 |
| triggered | 黄色 | 已触发 |
| resolved | 绿色 | 已回收 |
| dropped | 灰色 | 已废弃 |

---

## 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2026-04-07 | 初始版本，完整功能文档 |

---

如有问题或建议，请联系开发团队。
